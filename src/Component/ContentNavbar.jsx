import React from 'react'

const ContentNavbar = (props) => {
    return (
        <div>
            <a class="nav-link" href="/">{props.data}</a>
        </div>
    )
}

export default ContentNavbar
